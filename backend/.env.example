# Backend environment variables (FastAPI/YOLOv8)
# Copy to backend/.env and export (or use a process manager to load)

# Path to YOLOv8 weights file
MODEL_PATH=/models/best.pt

# Directory to store processed outputs (videos/JSON)
PROCESSED_VIDEOS_DIR=/app/processed_videos

# Directory containing original input videos
ORIGINAL_VIDEOS_DIR=/app/public
